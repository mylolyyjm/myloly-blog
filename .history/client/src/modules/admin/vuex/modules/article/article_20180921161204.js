const state = {
    article: {
        title,
        content,
        abstract,
        image_url,
        // tags,
        publish,
    }
}

const mutation = {
    getArticle(state, article_content) {
        state.
    },
}