const state = {
    article: {
        title,
        content,
        abstract,
        image_url,
        // tags,
        publish,
    }
}

const mutations = {
    getArticle(state, article_content) {
        state.article = article_content;
    },
}

const actions = {
    getArticle(context) {
        context.
    }
}